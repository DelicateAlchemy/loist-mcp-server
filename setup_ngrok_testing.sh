# Install ngrok for oEmbed testing
# brew install ngrok/ngrok/ngrok  # on macOS
# npm install -g ngrok              # or with npm

# Start ngrok tunnel to localhost:8080
# ngrok http 8080

# Copy the ngrok URL (e.g., https://abc123.ngrok.io)
# Set EMBED_BASE_URL to the ngrok URL for testing:
# export EMBED_BASE_URL=https://abc123.ngrok.io
